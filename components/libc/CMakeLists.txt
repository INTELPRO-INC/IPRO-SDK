__component_add_subdirectory_ifdef(CONFIG_COMPONENTS_NEW_LIBC_ENABLE newlibc)

# Note: Semihosting sources are included directly in newlibc when enabled.
# The semihosting/ folder can be removed entirely for production builds.
