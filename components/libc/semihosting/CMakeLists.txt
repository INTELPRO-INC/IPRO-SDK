# Semihosting sources are included directly in newlibc when enabled.
# See components/libc/newlibc/CMakeLists.txt
#
# This CMakeLists.txt is kept for potential future use where semihosting
# might be built as a separate library.
