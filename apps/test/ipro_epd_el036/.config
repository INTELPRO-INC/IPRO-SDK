CONFIG_IPRO7=y
CONFIG_CHIP="IPRO7"
CONFIG_CPU_ID="intelpro_ipro7"
CONFIG_EVB=y
CONFIG_BOARD="EVB"

CONFIG_FREERTOS_SUPPORT=y
CONFIG_COMPONENTS_FREERTOS_ENABLE=y
CONFIG_FREERTOS=y

CONFIG_LINK_FLASH=y

#
# Debug UART — must match the terminal. boot2 prints its banner at its own
# rate; the app reconfigures UART0 here, so a mismatch looks like the board
# hung right after the boot log.
#
CONFIG_PLATFORM_UART_BAUDRATE=921600

CONFIG_SHELL=y
# A full-panel refresh takes tens of seconds. Without this, shell_handler()
# runs the command inside shell_task and the console is frozen for the whole
# refresh — indistinguishable from a crash.
CONFIG_SHELL_EXEC_THREAD=y
CONFIG_COMPONENTS_NEW_LIBC_ENABLE=y
CONFIG_COMPONENTS_UTILITY_API_ENABLE=y
CONFIG_COMPONENTS_SOFTCRC_ENABLE=y

CONFIG_IPRO_LOG_ENABLE=y
CONFIG_LOG_DEFAULT_LEVEL_INFO=y
CONFIG_LOG_DEFAULT_LEVEL=3
CONFIG_LOG_COLORS=y
CONFIG_LOG_TIMESTAMP=y
CONFIG_LOG_BUFFER_SIZE=256
CONFIG_LOG_USE_MUTEX=y
CONFIG_LOG_OUTPUT_UART=y

CONFIG_EXCEPTION_DUMP=y
CONFIG_ENABLE_FRAME_POINTER=y
